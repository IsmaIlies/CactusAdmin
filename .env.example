# Base URL used inside HTML emails for loading images (logos, etc.)
# Set this to your public production domain so Outlook can fetch images.
# Copy this file to `.env` and adjust the value if needed.
VITE_PUBLIC_BASE_URL=https://admin.cactus-tech.fr
